module.exports = function(grunt) {
    grunt.registerTask('default', function() {
        grunt.log.writeln('Ice Blog now uses plain Jekyll and css/ice-blog.css. No Grunt build step is required.');
    });
};
